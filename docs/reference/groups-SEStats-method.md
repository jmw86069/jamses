# Get groups from SEStats

Extract group names from the SEStats object's sedesign.

## Usage

``` r
## S7 method for class <SEStats>
groups(object)
```

## Arguments

- x:

  An `SEStats` object

## Value

Character vector of group names

## Details

This is a read-only accessor that retrieves the group names from the
SEDesign object stored in the SEStats object.

## See also

Other SEStats objects: [`.class_list`](dot-class_list.md),
[`contrast_names,SEStats-method`](contrast_names-SEStats-method.md),
[`factors,SEStats-method`](factors-SEStats-method.md),
[`print,SEStats-method`](print-SEStats-method.md),
[`samples,SEStats-method`](samples-SEStats-method.md)
