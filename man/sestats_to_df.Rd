% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jam_sestats.R
\name{sestats_to_df}
\alias{sestats_to_df}
\title{Convert sestats to table summary}
\usage{
sestats_to_df(
  sestats,
  style = c("text", "integer"),
  dimname_order = c(3, 2, 1),
  rename_contrasts = FALSE,
  ...
)
}
\arguments{
\item{sestats}{\code{list} output from \code{se_contrast_stats()}}

\item{style}{\code{character} string indicating what values to use:
\itemize{
\item \code{"text"}: number of hits (number up, number down)
\item \code{"integer"}: only the integer number of hits
}}

\item{rename_contrasts}{\code{logical} indicating whether to rename
contrasts using \code{contrast2comp()}. The main benefit is reduction
in length of the string that describes each contrast.}

\item{...}{additional arguments are passed to \code{contrast2comp()}, which
is relevant when \code{rename_contrasts=TRUE}, relevant arguments include:
\itemize{
\item \code{contrast_delim="-"} to
\item \code{contrast_factor_delim="_"} to customize the delimiter between factors
in the input group names, for example \code{"Treatment1_Time1"} uses "_".
\item \code{comp_factor_delim=":"} to customize the delimiter between factors
\item \code{factor_order=NULL} to customize the order of factor comparisons
}}
}
\value{
\code{data.frame} with a summary of statistical hits per contrast,
assay_name, and threshold.
}
\description{
Convert sestats to table summary
}
\details{
Note: This function is intended to provide a simple data.frame
summary with the number of hits for each contrast, signal, cutoff.
It is still being tested, and updated for usability.

TODO: The order of \code{dimnames(hit_array)} should be user-customizable.
The series of dimnames in each lapply should use this order.
}
\examples{
if (FALSE) {
hitdf <- sestats_to_df(list(hit_array=hit_array));
hitdf_rowindex <- table(hitdf[[1]])[unique(hitdf[[1]])]
jamba::kable_coloring(
   hitdf[, -1, drop=FALSE],
   row.names=FALSE) \%>\%
   kableExtra::pack_rows(index=hitdf_rowindex)
}

}
\seealso{
Other jamses stats: 
\code{\link{ebayes2dfs}()},
\code{\link{format_hits}()},
\code{\link{handle_na_values}()},
\code{\link{hit_array_to_list}()},
\code{\link{process_sestats_to_hitim}()},
\code{\link{run_limma_replicate}()},
\code{\link{save_sestats}()},
\code{\link{se_contrast_stats}()},
\code{\link{sestats_to_dfs}()},
\code{\link{voom_jam}()}
}
\concept{jamses stats}
