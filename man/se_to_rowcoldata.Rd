% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/se_to_rowcoldata.R
\name{se_to_rowcoldata}
\alias{se_to_rowcoldata}
\title{Get SE colData and rowData}
\usage{
se_to_rowcoldata(se, verbose = FALSE, ...)
}
\arguments{
\item{se}{recognized object:
\itemize{
\item \code{SummarizedExperiment} or any object that inherits from this class
\item \code{SingleCellExperiment} because it inherits from \code{SummarizedExperiment}
\item \code{Seurat} which is converted to \code{SingleCellExperiment}
\item \code{ExpressionSet} and any object that inherits from this class, using
\code{Biobase::featureData()} and \code{Biobase::phenoData()}.
\item \code{NanoStringGeoMxSet} which uses \code{GeomxTools::sData()} or
\code{NanoStringNCTools::sData()} to define \code{colData_se}, otherwise is
handled equivalent to \code{ExpressionSet}.
}}

\item{...}{additional arguments are ignored}
}
\value{
\code{list} with two components:
\itemize{
\item \code{"colData_se"} as a \code{data.frame} with column metadata from \code{se}
\item \code{"rowData_se"} as a \code{data.frame} with row metadata from \code{se}
}
}
\description{
Get SE colData and rowData consistently for SummarizedExperiment,
SingleCellExperiment, Seurat data, and generic Biobase \code{eSet}
compatible objects that provide \code{featureData()} and \code{phenoData()}.
}
\details{
This function provides a straightforward way to return
the equivalent of \code{data.frame(check.names=FALSE, rowData(se))}
and \code{data.frame(check.names=FALSE, colData(se))} for several
types of object types.
\itemize{
\item It also defines \code{rownames()} and \code{colnames()} if either are missing.
\item When \code{rowData} has no annotation columns, it defines one column \code{"rows"}
using \code{rownames(se)}.
\item If slot name \code{"rowRanges"} exists, and \code{"rowData"} either does not
exist or has zero columns, it will use \code{rowRanges()}.
\item When \code{colData} has no annotation columns, it defines one column
\code{"columns"} using \code{colnames(se)}.
\item When input \code{se} class is \code{"Seurat"}, it converts the object with
\code{Seurat::as.SingleCellExperiment()}
\item Any other object uses \code{Biobase::featureData()} for \code{"rowData_se"},
and \code{Biobase::phenoData()} for \code{"colData_se"}.
}

To verify the logic used at each step, set \code{verbose=TRUE}.

For Class \code{"NanoStringGeoMxSet"} defined in \code{GeomxTools}, the
\code{colData_se} is defined using \code{GeomxTools::sData()} in order to
return the combined \code{data.frame} with \code{protocolData()} and \code{pData()}
together. Accordingly, it is possible to have duplicated colnames,
which becomes a problem for many downstream tools, so the second
instance of any duplicated colnames have \code{"_v1"} added by using
\code{jamba::makeNames(x, renameFirst=FALSE)}. The first instance of
each duplicated colname is not renamed. Use \code{verbose=TRUE} to confirm
when duplicated columns are detected and renamed.
}
\seealso{
Other jamses SE utilities: 
\code{\link{make_se_test}()},
\code{\link{se_collapse_by_column}()},
\code{\link{se_collapse_by_row}()},
\code{\link{se_detected_rows}()},
\code{\link{se_normalize}()},
\code{\link{se_rbind}()}
}
\concept{jamses SE utilities}
