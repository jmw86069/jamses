% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jam_sestats.R
\name{format_hits}
\alias{format_hits}
\title{Format list of hit vectors into summary counts}
\usage{
format_hits(hits, style = c("text", "integer", "vector"), ...)
}
\arguments{
\item{hits}{\code{list}, \code{array}, or \code{vector} with values \code{c(-1, 1)}
indicating hits down, or hits up, respectively. When any vector
contains only \code{NA} values, then \code{NA} is also returned. This
distinction is as follows:
\itemize{
\item \code{NA} values indicate the statistical contrast was not performed,
which happens when \code{se_contrast_stats()} arguments for interaction
contrasts differ from pairwise contrasts, for example \code{int_adjp_cutoff},
\code{int_p_cutoff}, or \code{int_fold_cutoff}.
\item \code{length==0} indicates the statistical contrast was performed, and
there were no statistical hits for the given cutoffs.
}}

\item{style}{\code{character} string indicating the output format:
\itemize{
\item \code{"text"}: \code{character} string with \code{"hits(hits up, hits down)"}.
\item \code{"integer"}: \code{integer} number of hits, or \code{NA} when the test was
not performed.
\item \code{"vector"}: \code{integer} vector with names \verb{("hit", "up", "down")}.
}}

\item{...}{additional arguments are ignored.}
}
\value{
the same data type as input, where the hit vector is replaced
with a single value summarizing the hits. The data types have
three expected options:
\itemize{
\item \code{array}: when used with \code{hit_array} data from \code{se_contrast_stats()}
\item \code{list}: when used on a particular subset of \code{hit_array} data
\item \code{numeric}: when used with a single contrast
}
}
\description{
Format list of hit vectors into summary counts
}
\details{
This function is used by \code{sestats_to_df()},
to summarize hits for each contrast into one of these formats:
\itemize{
\item string summary of statistical hits with \code{"hits,up,down"}
when \code{style="text"}, for example: \code{"623 hits (267 up, 379 down)"}
\item integer count of statistical hits when \code{style="integer"},
for example: \code{623}.
\item \code{vector} of integer counts for \code{c("hits", "up", "down")},
for example: \code{c(hits=623, up=267, down=379)}.
}

The function may be useful outside of \code{sestats_to_df()} so it
is exported as a convenience function.
}
\examples{
set.seed(123)
hitlist <- list(
   `groupA-groupB`=sample(c(-1, 1), size=25, replace=TRUE),
   `groupA-groupC`=sample(c(-1, 1), size=50, replace=TRUE))
format_hits(hitlist, style="text")
format_hits(hitlist, style="integer")

}
\seealso{
Other jamses stats: 
\code{\link{ebayes2dfs}()},
\code{\link{handle_na_values}()},
\code{\link{hit_array_to_list}()},
\code{\link{process_sestats_to_hitim}()},
\code{\link{run_limma_replicate}()},
\code{\link{save_sestats}()},
\code{\link{se_contrast_stats}()},
\code{\link{sestats_to_dfs}()},
\code{\link{sestats_to_df}()},
\code{\link{voom_jam}()}
}
\concept{jamses stats}
