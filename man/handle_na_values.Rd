% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jam_secontrasts.R
\name{handle_na_values}
\alias{handle_na_values}
\title{Handle NA values in a numeric matrix}
\usage{
handle_na_values(
  x,
  idesign,
  handle_na = c("full1", "full", "partial", "none", "all"),
  na_value = 0,
  na_weight = 0,
  return_weights = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{x}{\code{numeric} matrix}

\item{idesign}{\code{numeric} matrix with \code{rownames(idesign)} equal to
\code{colnames(x)}, containing \code{0} or \code{1} to fill the design matrix.}

\item{handle_na}{\code{character} string to determine the method used
to handle NA values in \code{x}.}

\item{na_value}{\code{numeric} or \code{NA} used to handle NA values.}

\item{na_weight}{\code{numeric} weight between \code{0} and \code{1} used for \code{NA}
values in the weight matrix, used when \code{return_weights=TRUE}.}

\item{return_weights}{\code{logical} indicating whether to include a
weight matrix as an attribute with name \code{"weights"}.}

\item{verbose}{\code{logical} indicating whether to print verbose output.}

\item{...}{additional arguments are ignored.}
}
\value{
\code{numeric} matrix with equal dimensions as input \code{x},
where \code{NA} values have been handled as defined by \code{handle_na}.
}
\description{
Handle NA values in a numeric matrix
}
\details{
This function provides reasonable alternatives intended to
manage the presence of missing data encoded as \code{NA} values
in a numeric matrix.

The alternatives are defined by argument \code{handle_na}:
\itemize{
\item \code{"full"}: Retain \code{NA} values, except when an entire group is \code{NA}
it is replaced with \code{na_value}. This option is intended for sparse data
where non-NA values are accepted as real measurements
for each group, and where a group with all NA values should be
retained for statistical contrasts by assigning \code{na_value}.
This method essentially keeps the data as-is, except when groups
are otherwise entirely \code{NA} the value \code{na_value} is used in
order to retain any relevant contrasts that involve this group.
\item \code{"full1"}: Similar to \code{"full"}, retain \code{NA} values, except
when an entire group is \code{NA}, then replace only one entry
with \code{na_value}. This option is intended to keep data as-is,
except to retain groups that are otherwise entirely \code{NA}.
In these groups, only one \code{na_value} is used in order to
prevent the group from contributing toward group variability
or dispersion calculations.
\item \code{"partial"}: Replace \code{NA} values with \code{na_value}, except
when an entire group is \code{NA} the entire group is kept at \code{NA}.
\item \code{"all"}: Replace all \code{NA} values with \code{na_value}.
\item \code{"none"}: Perform no replacement of \code{NA} values.
}
}
\seealso{
Other jamses stats: 
\code{\link{ebayes2dfs}()},
\code{\link{format_hits}()},
\code{\link{hit_array_to_list}()},
\code{\link{process_sestats_to_hitim}()},
\code{\link{run_limma_replicate}()},
\code{\link{save_sestats}()},
\code{\link{se_contrast_stats}()},
\code{\link{sestats_to_df}()},
\code{\link{sestats_to_dfs}()},
\code{\link{voom_jam}()}
}
\concept{jamses stats}
