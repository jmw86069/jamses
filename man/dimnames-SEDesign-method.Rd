% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jam_sedesign.R
\name{dimnames,SEDesign-method}
\alias{dimnames,SEDesign-method}
\title{Get dimnames from SEDesign}
\usage{
## S7 method for class <SEDesign>
dimnames(x)
}
\arguments{
\item{x}{An \code{SEDesign} object}
}
\value{
\code{list} with named elements: \verb{$samples}, \verb{$groups}, \verb{$contrasts}
}
\description{
Extract the dimension names (samples, groups, contrast_names) from an
SEDesign object, similar to how \code{dimnames()} works on arrays or matrices.
}
\details{
Returns a \code{list} with three named elements:
\itemize{
\item \code{samples}: Sample names (from \code{samples(x)})
\item \code{groups}: Group names (from \code{groups(x)})
\item \code{contrasts}: Contrast names (from \code{contrast_names(x)})
}

This provides a unified way to access all dimension names from a SEDesign
object, consistent with base R's \code{dimnames()} function for arrays and
matrices.
}
\examples{
if (FALSE) {
  dimnames(sedesign)
}

}
