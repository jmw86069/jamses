% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jam_sestats_hm.R
\name{call_fn_ellipsis}
\alias{call_fn_ellipsis}
\title{Call function using safe ellipsis arguments}
\usage{
call_fn_ellipsis(FUN, ...)
}
\arguments{
\item{FUN}{\code{function} that should be called with arguments
in \code{...}}

\item{...}{arguments are passed to \code{FUN()} in safe manner.}
}
\description{
Call function using safe ellipsis arguments
}
\details{
This function is a wrapper function intended to help
pass ellipsis arguments \code{...} from a parent function
to an external function in a safe way.
It will only include arguments from \code{...} that are
recognized by the external function.

When the external function FUN arguments \code{formals()}
includes ellipsis \code{...}, then the \code{...} will be passed
as-is without change.

When the external function FUN arguments \code{formals()}
does not include ellipsis \code{...}, then only named
arguments in \code{...} that are recognized by FUN
will be passed, as defined by \code{names(formals(FUN))}.

Note that arguments must be named.
}
\examples{
new_mean <- function(x, trim=0, na.rm=FALSE) {
   mean(x, trim=trim, na.rm=na.rm)
}
x <- c(1, 3, 5, NA);
new_mean(x, na.rm=TRUE);
tryCatch({
   new_mean(x, na.rm=TRUE, color="red");
}, error=function(e){
   print(e);
})

call_fn_ellipsis(new_mean, x=x, na.rm=TRUE, color="red")
call_fn_ellipsis(new_mean, x=x, color="red")

}
