% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jam_contrast2comp.R
\name{stack_contrasts}
\alias{stack_contrasts}
\title{Stack a vector of contrasts into a full string}
\usage{
stack_contrasts(imgroups, contrast_delim = "-", ...)
}
\arguments{
\item{imgroups}{\code{character} vector of group names.}

\item{contrast_delim}{\code{character} string used as a delimiter between
group names to indicate a statistical contrast.}

\item{...}{additional arguments are ignored.}
}
\value{
\code{character} vector with fully written contrasts.
}
\description{
Stack a vector of contrasts into a full string
}
\details{
This is an internal function called by \code{contrast2comp()} and
\code{comp2contrast()}. It takes a single vector of groups, with
2^(integer) number of entries, and combines them into a
series of pairwise contrasts.

For example:
\itemize{
\item \verb{A, B} becomes \code{A-B}
\item \verb{A, B, C, D} becomes \code{(A-B)-(C-D)}
\item \verb{A, B, C, D, E, F, G, H} becomes \code{((A-B)-(C-D))-((E-F)-(G-H))}
}
}
\examples{
stack_contrasts(head(LETTERS, 2))
stack_contrasts(head(LETTERS, 4))
stack_contrasts(head(LETTERS, 8))
stack_contrasts(head(LETTERS, 16))
tryCatch({
   stack_contrasts(head(LETTERS, 5))
}, error=function(e){
   print(e)
})

}
