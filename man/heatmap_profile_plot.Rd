% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jam_sestats_hm_profile.R
\name{heatmap_profile_plot}
\alias{heatmap_profile_plot}
\title{Profile Line Plot for Heatmap of SummarizedExperiment data}
\usage{
heatmap_profile_plot(
  hm,
  display_profiles = c("rows", "group"),
  summarize_column_split = TRUE,
  se = NULL,
  rowData_colnames = NULL,
  profile_color = "#88888844",
  mean_color = "#000000FF",
  include_count = TRUE,
  row_type = "Gene",
  strip.position = "right",
  base_size = 10,
  panel.background.fill = "grey97",
  facet_ncol = 1,
  return_type = c("ggplot", "list"),
  ...
)
}
\arguments{
\item{hm}{\code{ComplexHeatmap::Heatmap} object, intended to use output
from \code{heatmap_se()}.
It uses the data within the heatmap to produce the plot,
therefore all data centering is used as defined in \code{heatmap_se()}.}

\item{display_profiles}{\code{character} indicating which profiles to include:
\itemize{
\item 'rows': include a profile line for each row
\item 'group': include a profile line for the row group.
}}

\item{summarize_column_split}{\code{logical} default TRUE, whether to summarize
values in \code{column_split} by taking the mean across replicates (columns)
within each column grouping.
\itemize{
\item When \code{summarize_column_split=FALSE} it will keep replicates as
individual points, which may appear more consistent with the heatmap.
}}

\item{rowData_colnames}{\code{character} vector, currently ignored.
In future it may be used to apply different row groupings.}

\item{profile_color}{\code{character} string, default is partially transparent,
light grey. This color is used for the line profile color.}

\item{include_count}{\code{logical} default TRUE, whether to include the
row count in the label for each row group.
\itemize{
\item It uses \code{sep="\\n"} by default between the row group label,
and the row count label. So to make the label all appear on one line,
use \code{sep=" "}.
}}

\item{row_type}{\code{character} string, default \code{"Gene"} used when
\code{include_count=TRUE}, so the label becomes \code{"11 Genes"}
The label should be singular, and the 's' is added when there
is more than 1 row in the group.}

\item{strip.position}{\code{character} string, default 'right', places the
ggplot2 facet strip label on the right side.
The other common option is 'top', which is better for long labels,
but requires a taller graphics device to accomodate the number of
row groups.}

\item{base_size}{\code{numeric} base default font size for the ggplot2 theme,
default 10 is relatively small, suitable for analysis.
Use a larger number for figures, but adjust the graphics device size
accordingly.}

\item{panel.background.fill}{\code{character} color used for the ggplot2
panel background color, default is very light grey (lighter than
ggplot2 default). The grey color makes it slightly easier to recognize
each plot panel.}

\item{...}{additional arguments are passed to internal functions,
specifically \code{colorjam::theme_jam()} which defines the ggplot2 theme.
The theme can be replaced by adding \code{ggplot2::theme_bw()} or something
else for example.}
}
\value{
\code{ggplot} object suitable for plotting or further customization.
\itemize{
\item When \code{return_type='list'} it returns a \code{list} with 'hmtall'
which is a \code{data.frame} containing data used for the ggplot.
If display_profiles includes 'group' the \code{list} will contain 'hmtall4'
which is a \code{data.frame} containing the row group mean values.
\item When return_type='ggplot' (default) the \code{output@data}
can also be used to inspect the underlying data.
}
}
\description{
Convert Heatmap of SummarizedExperiment data into Profile Line Plot,
with row and column groups and facets as relevant.
}
\details{
It uses the data within the heatmap to produce the plot,
therefore all data centering is used as defined in \code{heatmap_se()}.
This plot is intended to produce a line plot equivalent to
the same data shown in heatmap form. Sometimes the linear plot
is more visually intuitive, and certainly is more effective
at conveying magnitudes than color gradient.

Row groups are detected by calling \code{jamba::heatmap_row_order()}.
If there are no row groups, all rows are placed into the same group.

Column groups are also detected by calling \code{jamba::heatmap_column_order()}.
The argument \code{summarize_column_split=TRUE} will optionally calculate
group mean per column group, but the default is \code{FALSE}.
}
\examples{
hm1 <- heatmap_se(se,
   rowData_colnames="Class",
   row_split=9,
   column_split="group",
   show_left_annotation_name="top",
   left_annotation_name_rot=150,
   sample_color_list=list(group=c(groupA="red")))
hm1
heatmap_profile_plot(hm1, summarize_column_split=FALSE);

se <- jamses::make_se_test(nrow=200, ngroups=4, nreps=8)
hm <- jamses::heatmap_se(se,
   rowData_colnames="Class",
   apply_hm_column_title=TRUE,
   column_split="group",
   cluster_row_slices=TRUE,
   controlSamples=head(colnames(se), 8),
   row_split=12)
ComplexHeatmap::draw(hm)

HeatmapProfilePlot(hm, strip.position="top", sep=" ")

HeatmapProfilePlot(hm, FALSE, strip.position="top", sep=" ")

HeatmapProfilePlot(hm, FALSE)

}
\seealso{
Other jamses heatmaps: 
\code{\link{detect_heatmap_components}()},
\code{\link{heatmap_column_group_labels}()},
\code{\link{heatmap_se}()}
}
\concept{jamses heatmaps}
