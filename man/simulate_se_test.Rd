% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jam_simulate_se_test.R
\name{simulate_se_test}
\alias{simulate_se_test}
\title{Simulate SummarizedExperiment tests}
\usage{
simulate_se_test(
  ngroups = 4,
  nreps = 5,
  nrow = 250,
  multiplier = 1.5,
  offset = 7,
  batch_ex = 0.15,
  hit_exp = 2,
  hit_fraction = 1/2,
  hit_max = 2.8,
  noise_factor = 1,
  seed = 123,
  assay_name = "counts",
  sparsity = 0,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{ngroups}{\code{integer} number of experimental groups}

\item{nreps}{\code{integer} number of replicates per group, can be used
to provide the number of replicates for each group in order.}

\item{nrow}{\code{integer} number of rows (measurements)}

\item{multiplier}{\code{numeric} value multiplied by \code{rnorm()} to adjust
the magnitude of values produced, default 1.}

\item{offset}{\code{numeric} value added to the output of \code{rnorm()}, default 7.}

\item{batch_ex}{\code{numeric} adjustment for batch effect, default \code{0.15}
is a moderate batch effect, it would require adjustment or
blocking or covariate for effective analysis.}

\item{hit_exp}{\code{numeric} default 2, adjust the "sharpness" of
differential changes, higher values are sharper, with fewer
strong hits.}

\item{hit_fraction}{\code{numeric} value between 0 and 1 indicating the
fraction of rows to simulate as having a fold change, default 1/2.}

\item{hit_max}{\code{numeric} maximum value for a simulated fold change,
default 2.8, intended to be interpreted as log2 of a 7-fold change.}

\item{noise_factor}{\code{numeric} multiplied by \code{rnorm()} to add additional
noise.}

\item{seed}{\code{numeric} passed to \code{set.seed()} when provided, for
reproducible random output.}

\item{assay_name}{\code{character} name to use for the assay name in the output.}

\item{sparsity}{\code{numeric} value from 0 to 1, default 0, indicating the
fraction of values that are converted to \code{NA} to simulate sparse
data measurements. It can be provided as a vector and applied to
each group in order.
In some proteomics datasets, control samples may be substantially
more sparse than other groups, for example if the control is
non-targeted IP, or negative control. In this case, data can
be simulated by using \code{sparsity=c(0.6, 0, 0)} for three groups.}

\item{verbose}{\code{logical} indicating whether to print verbose output.}

\item{...}{additional arguments are ignored.}
}
\value{
\code{SummarizedExperiment} object
}
\description{
Simulate SummarizedExperiment tests, with adjustable batch effect.
}
\details{
This function extends \code{make_se_test()} in two ways:
\enumerate{
\item For four-group scenario, it will generate two-factor changes.
\item Batch effects are generated, adjustable with \code{batch_ex}.
Use \code{batch_ex=0} for no batch effect. See examples for
demonstration of centering within batch as visual cue that
batch effect is well-defined.
}
}
\examples{
# batch effects
seb <- simulate_se_test();
hmb <- heatmap_se(seb,
   controlSamples=colnames(seb)[c(1:5, 11:15)],
   column_title="With batch effect\nglobal-centered")
hmb

# when centering versus a sparse control group, some values can be lost:
hmbg <- heatmap_se(seb,
   controlSamples=colnames(seb)[c(1:5, 11:15)],
   centerby_colnames="batch",
   column_title="With batch effect\ncentered by batch")
hmb + hmbg

hmbc <- heatmap_se(seb,
   correlation=TRUE,
   controlSamples=colnames(seb)[c(1:5, 11:15)],
   column_title="With batch effect\nglobal-centered")
hmbc

hmbgc <- heatmap_se(seb,
   correlation=TRUE,
   controlSamples=colnames(seb)[c(1:5, 11:15)],
   centerby_colnames="batch",
   column_title="With batch effect\ncentered by batch")
hmbgc
hmbc + hmbgc

}
\seealso{
Other jamses SE utilities:
\code{\link[=geomx_to_se]{geomx_to_se()}},
\code{\link[=make_se_test]{make_se_test()}},
\code{\link[=se_collapse_by_column]{se_collapse_by_column()}},
\code{\link[=se_collapse_by_row]{se_collapse_by_row()}},
\code{\link[=se_detected_rows]{se_detected_rows()}},
\code{\link[=se_normalize]{se_normalize()}},
\code{\link[=se_rbind]{se_rbind()}},
\code{\link[=se_to_assay_data]{se_to_assay_data()}},
\code{\link[=se_to_assay_names]{se_to_assay_names()}},
\code{\link[=se_to_rowcoldata]{se_to_rowcoldata()}}
}
\concept{jamses SE utilities}
